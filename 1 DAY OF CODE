#include <bits/stdc++.h>

using namespace std;
int main()
{
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    cout.tie(0);
    int n,d;
    cin>>n>>d;
    vector<int> a(n);
    int i;
    for(i=0;i<n;i++)
    cin>>a[i];
    for(i=d;i<a.size();i++)
    cout<<a[i]<<" ";
    for(i=0;i<d;i++)
    cout<<a[i]<<" ";
    cout<<endl;
    return 0;
}
